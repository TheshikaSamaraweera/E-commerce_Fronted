import { ISliceOptions } from './types';
/**
 * createSlice(options)
 * @example
 * createSlice({
 *   reducer: (state, action) => {
 *     if(action === 'increment') return state + 1;
 *     if(action === 'decrement') return state - 1;
 *     return state;
 *   },
 *   initialState: 0, // optional
 *   debugName: 'Counter' // optional
 * })
 */
export default function createSlice<TState = any, TActions = any>(options: ISliceOptions<TState, TActions>): import("./types").ISliceStore<TState, TActions>;
